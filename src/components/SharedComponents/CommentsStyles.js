import styled from 'styled-components';

export const CountCommentsAndIcon = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 50%;

    span {
        font-family: var(--default-font);
        word-break: break-all;
        font-size: 11px;
    }

`